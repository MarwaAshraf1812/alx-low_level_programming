#include "main.h"

/**
 * _isupper -  checks for uppercase character.
 * @c: c -  Variable
 * Return: Always 0.
 */
int _isupper(int c)
{
	for (c = 65; c <= 90; c++)
	{
		return (1);
	}
	return (0);
}
