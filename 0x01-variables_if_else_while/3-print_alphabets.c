#include <stdio.h>

/**
 * main - write lower and uppercase alph
 *
 * Return: 0 (success)
 *
 */
int main(void)
{
	char alpha;

	for (alpha = 'a'; alpha <= 'a'; alpha++)
		putchar(alpha);
	for (alpha = 'A'; alpha <= 'Z'; alpha++)
		putchar(alpha);

	putchar('\n');
	return (0);
}
